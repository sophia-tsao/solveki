info:
  name: Get topics of a course
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/courses/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
